# Workspace Instructions

- Build the ClassOneSystem replica with React + Vite.
- Keep all imagery as placeholders; preserve the live site layout, colors, spacing, and typography.
- Verify the app at `http://localhost:5173` after each major change.
- Keep navigation and common product routes working inside the local app.