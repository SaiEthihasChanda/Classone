paths-ignore:
  # Firebase web client config is intentionally public — it is a project
  # identifier, not a credential. Access is enforced by Firestore/Storage
  # Security Rules, not by keeping this value secret. See:
  # https://firebase.google.com/docs/projects/api-keys
  - 'src/lib/firebase.js'
